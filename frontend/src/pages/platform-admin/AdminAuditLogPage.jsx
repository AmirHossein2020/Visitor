import {useEffect,useState} from "react";import {adminAuditLog} from "../../services/platformAdmin";import {AdminLayout,StateBox} from "../../components/platform-admin/AdminUI";
const labels={account_status_changed:"تغییر وضعیت حساب",role_changed:"تغییر نقش",subscription_activated:"فعال‌سازی اشتراک",subscription_extended:"تمدید اشتراک",subscription_cancelled:"لغو اشتراک",subscription_adjusted:"تنظیم دستی اشتراک",plan_created:"ایجاد پلن",plan_updated:"تغییر پلن",payment_approved:"تأیید پرداخت",payment_rejected:"رد پرداخت",platform_settings_updated:"تغییر تنظیمات"};
export default function AdminAuditLogPage(){const [filters,setFilters]=useState({actor:"",action:"",date_from:"",date_to:""}),[data,setData]=useState(),[error,setError]=useState("");const load=()=>adminAuditLog(new URLSearchParams(Object.fromEntries(Object.entries(filters).filter(([,v])=>v))).toString()).then(setData).catch(()=>setError("دریافت گزارش فعالیت ممکن نشد."));useEffect(load,[]);return <AdminLayout title="گزارش فعالیت مدیران" subtitle="تاریخچه فقط‌خواندنی اقدامات حساس"><div className="mb-5 grid gap-3 rounded-2xl bg-white p-4 sm:grid-cols-2 xl:grid-cols-5"><input className="admin-input" placeholder="ایمیل مدیر" value={filters.actor} onChange={e=>setFilters({...filters,actor:e.target.value})}/><select className="admin-input" value={filters.action} onChange={e=>setFilters({...filters,action:e.target.value})}><option value="">همه عملیات</option>{Object.entries(labels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><input className="admin-input" type="date" value={filters.date_from} onChange={e=>setFilters({...filters,date_from:e.target.value})}/><input className="admin-input" type="date" value={filters.date_to} onChange={e=>setFilters({...filters,date_to:e.target.value})}/><button className="admin-button bg-slate-900 text-white" onClick={load}>اعمال فیلتر</button></div><StateBox loading={!data&&!error} error={error} empty={data?.results?.length===0}/><div className="space-y-3">{data?.results?.map(x=><article className="rounded-2xl bg-white p-5 ring-1 ring-slate-200" key={x.id}><div className="flex flex-wrap justify-between gap-2"><b>{labels[x.action]||x.action}</b><time className="text-sm text-slate-500">{new Intl.DateTimeFormat("fa-IR",{dateStyle:"medium",timeStyle:"short"}).format(new Date(x.created_at))}</time></div><p className="mt-2 text-sm" dir="ltr">{x.actor_email||"سیستم"}</p><p className="mt-1 text-sm text-slate-600">{x.target_type}: {x.target_display}</p>{x.reason&&<p className="mt-2 rounded-lg bg-slate-50 p-3 text-sm">دلیل: {x.reason}</p>}</article>)}</div></AdminLayout>}
